{
  "name": "nongyak-kb",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "fetch": "bun run src/fetch.ts",
    "normalize": "bun run src/normalize.ts",
    "export": "bun run src/export-ttl.ts && bun run src/export-graph.ts",
    "cli": "bun run src/cli.ts",
    "test": "bun test"
  },
  "dependencies": {
    "fast-xml-parser": "^5.10.1",
    "n3": "^2.1.1"
  },
  "devDependencies": {
    "@types/n3": "^1.26.1",
    "bun-types": "^1.3.14",
    "typescript": "^7.0.2"
  }
}
